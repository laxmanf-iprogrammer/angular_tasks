export class Course {
    public name: string;
    public description: string;
    public courseImagePath: string;


    constructor(name: string, desc: string, courseImagepath: string) {
        this.name = name;
        this.description = desc;
        this.courseImagePath = "abc"
    }

}